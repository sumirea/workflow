// @sumirea/sdk — the public, stable API.
//
// The sdk composes @sumirea/core with a selection of @sumirea/adapters into the
// one entry point embedders and surfaces are meant to use. It is the seam
// between "the product" and "everything that consumes it": surfaces depend on
// the sdk and never reach past it into core.
//
// Placeholder: no public API surface defined yet. See ARCHITECTURE.md.
export {};
