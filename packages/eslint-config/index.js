// Shared ESLint flat-config preset for the Sumirea monorepo.
//
// Placeholder: rule sets are intentionally not defined yet. The concrete
// configuration (TypeScript rules, import boundaries enforcing the
// architecture's dependency direction, etc.) will be added via RFC.
/** @type {import('eslint').Linter.Config[]} */
export default [];
