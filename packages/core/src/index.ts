// @sumirea/core — the workflow engine.
//
// This package contains the domain model, step execution, and orchestration
// that ARE the product. It is pure and surface-agnostic: no network, no
// filesystem, no direct clock or randomness. Everything external arrives
// through ports that adapters implement.
//
// It depends only on @sumirea/schema. It must never import @sumirea/adapters.
//
// Placeholder: no engine code yet. See ARCHITECTURE.md.
export {};
