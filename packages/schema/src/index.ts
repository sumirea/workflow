// @sumirea/schema — the contract layer.
//
// This package defines the canonical workflow definition format, the shared
// types every other package speaks, and runtime validation for them. It is the
// foundation of the dependency graph and depends on nothing else internal.
//
// Placeholder: no types are defined yet. See ARCHITECTURE.md.
export {};
