// @sumirea/ui — shared presentational components.
//
// Surface-agnostic, reusable UI building blocks shared by visual surfaces (the
// extension, the docs site, and any future web app). Components are
// presentational only: they render state and emit intent, they do not contain
// workflow logic.
//
// Placeholder: no components defined yet. See ARCHITECTURE.md.
export {};
