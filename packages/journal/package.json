{
  "name": "@sumirea/journal",
  "version": "0.0.0",
  "private": true,
  "description": "An in-memory, append-only record of Runtime lifecycle events — a pure consumer of @sumirea/core.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist .turbo *.tsbuildinfo",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "tsc --build && node --test \"test/*.test.mjs\""
  },
  "dependencies": {
    "@sumirea/core": "workspace:*"
  },
  "devDependencies": {
    "@sumirea/eslint-config": "workspace:*",
    "@sumirea/typescript-config": "workspace:*",
    "typescript": "^5.5.0"
  }
}
